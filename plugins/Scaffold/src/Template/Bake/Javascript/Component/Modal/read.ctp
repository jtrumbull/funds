/*
 |------------------------------------------------------------------------------
 | App\<%= $pluralName %>\Modal: Read
 |------------------------------------------------------------------------------
 */

(function(win, doc, $, _, Backbone, Moment, App, <%= $pluralName %>){
  
  'use strict';
  
  var ReadModal = App.ReadModal.extend({
    
  });
  
  <%= $pluralName %>.ReadModal = ReadModal;
  
}(window, document, jQuery, _, Backbone, moment, App, App.<%= $pluralName %>));
